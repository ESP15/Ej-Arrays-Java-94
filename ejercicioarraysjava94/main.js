const letras = ['a', 'b', 'c', 'd', 'e'];

console.log("Recorriendo el array de letras:");
letras.forEach(letra => {
    console.log(letra);
});

let numero = 5;

console.log("\nIterando la variable 'numero' hasta que sea 0:");
while (numero > 0) {
    console.log(numero);
    numero--;
}